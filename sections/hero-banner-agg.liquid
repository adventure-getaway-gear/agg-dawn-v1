{% schema %}
{%- comment -%} Test sync for Shopify {%- endcomment -%}
{
  "name": "Hero Banner AGG",
  "settings": [
    {
      "type": "image_picker",
      "id": "hero_image",
      "label": "Hero Image"
    {%- comment -%} Test sync for Shopify {%- endcomment -%}
    {% schema %}
    {
      "id": "headline",
      "label": "Headline",
      "default": "Adventure Awaits"
    {%- comment -%} Test sync for Shopify {%- endcomment -%}
    {% schema %}
    {
      "name": "Hero Banner AGG",
      "settings": [
        {
          "type": "image_picker",
          "id": "hero_image",
          "label": "Hero Image"
        },
        {
          "type": "text",
          "id": "headline",
          "label": "Headline",
          "default": "Adventure Awaits"
        },
        {
          "type": "text",
          "id": "subheadline",
          "label": "Subheadline",
          "default": "Gear up for your next getaway"
        },
        {
          "type": "url",
          "id": "cta_link",
          "label": "CTA Link"
        },
        {
          "type": "text",
          "id": "cta_text",
          "label": "CTA Text",
          "default": "Shop Now"
        }
      ]
    }
    {% endschema %}
